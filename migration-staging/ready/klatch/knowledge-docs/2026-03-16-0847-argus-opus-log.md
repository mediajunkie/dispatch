# Argus Session Log — 2026-03-16 08:47

**Model:** Opus 4.6
**Branch:** `claude/audit-and-planning-xn2w7`
**Starting point:** main at `5bb1ca7`

---

## 08:47 — Session start

Synced with origin. Key changes since yesterday evening:
- Sidebar design spec published: `docs/plans/SIDEBAR.md`
- GitHub issue #8 created for sidebar redesign
- Logbook March 15 entry completed by Calliope
- Blog post updated with AXT.md link

Read COORDINATION.md: Round 6 already pushed yesterday (13 tests, awaiting merge). Round 7 assigned: sidebar redesign tests for the new `type` column, klatch-requires-project constraint, sidebar grouping, and accordion behavior.

Read `docs/plans/SIDEBAR.md`: clear spec with glossary, wireframe, data model (type column: 'chat' | 'klatch'), project membership rules, accordion behavior.

No new mail for Argus.

Plan: Write Round 7 tests speculatively against the planned implementation. Server tests for data model, client tests for UI behavior. Tests will fail until Daedalus implements — that's expected per the assignment.

## 09:10 — Round 7: sidebar redesign speculative tests

### Server tests (11 total in `round7-sidebar-redesign.test.ts`)

**1. Type column migration (5):**
- Existing channels default to type 'chat'
- createChannel defaults to type 'chat'
- Can create a channel with type 'klatch'
- getAllChannelsEnriched returns type field
- Enriched query distinguishes chats from klatches in same project

**2. Klatch-project constraint (4):**
- Creating klatch without projectId → 400
- Creating klatch with projectId → 201
- Removing project from klatch via PATCH → 400
- Creating chat without projectId → 201 (allowed)

**3. Unassigned excludes klatches (2):**
- Klatches always have project, never appear in unassigned set
- Imported channels default to type 'chat'

### Client tests (10 total in `SidebarRedesign.test.tsx`)

**1. Chats above klatches within a project (4):**
- Renders "Chats" section header
- Renders "Klatches" section header
- Chats appear before klatches in DOM order
- Chats use @ prefix, klatches use # prefix

**2. Unassigned section — chats only (2):**
- Shows unassigned chats
- Does not show klatches in unassigned

**3. Accordion behavior (3):**
- First project expanded by default, others collapsed
- Expanding a project collapses the previously-open one
- Clicking active project collapses it

**4. Unassigned always visible (1):**
- Unassigned section stays visible regardless of accordion state

### Results
- **Speculative tests:** 8/11 server fail, 10/10 client fail (expected — implementation not yet landed)
- **Existing suite:** 529 server + 106 client = 635 total, zero regressions
- 3 server tests pass already (those testing existing behavior that won't change)

## 20:00 — Round 7 verification + Round 8

Synced with origin after massive day of Daedalus implementation:
- Sidebar redesign shipped (type column, accordion UI, chat/klatch sections)
- 5-layer prompt assembly (project memory as layer 3)
- Memory column on projects table
- Channel addendum hidden for chats
- Sorting by last activity

### Round 7 speculative test fixes (now passing)

**Server (2 fixes):**
- klatch-requires-project constraint is **UI-level, not API-enforced** — server returns 201/200, not 400. Updated test expectations and descriptions.

**Client (1 fix):**
- Auto-expand `useMemo` in `ChannelSidebar` re-opens first project when `expandedProject` is null. Updated "clicking active project collapses it" test to expect Alpha stays visible.

**Result:** 11/11 server + 10/10 client = 21/21 Round 7 tests passing.

### Round 8: project memory + prompt assembly (24 tests)

File: `packages/server/src/__tests__/round8-project-memory.test.ts`

**1. Project CRUD with memory (6):**
- createProject with memory stores and returns it
- createProject defaults memory to empty string
- getProject returns memory field
- updateProject with memory updates the field
- updateProject without memory preserves existing
- getAllProjects returns memory for all

**2. Import stores memory at project level (4):**
- Claude Code: MEMORY.md → project.memory, not instructions
- claude.ai: project memories → project.memory
- claude.ai: global account memories merged with project memories
- Instructions contain only CLAUDE.md / prompt_template, no memory

**3. 5-layer prompt assembly (6):**
- Memory appears with "Project memory:" header
- Memory appears AFTER instructions, BEFORE channel addendum
- Empty memory → no "Project memory:" in output
- Whitespace-only memory → no "Project memory:" in output
- Memory truncated at 8000 chars
- All 5 layers present in correct order for imported channel with project

**4. Legacy fallback (3):**
- Channel WITH projectId: kit briefing skips memoryMd/claudeMd
- Channel WITHOUT projectId: kit briefing injects memoryMd/claudeMd
- Channel WITHOUT projectId, no memoryMd: no memory section

**5. Prompt debug endpoint (5):**
- Shows ACTIVE when project has memory
- Shows EMPTY when project has no memory
- Shows INACTIVE when no project linked
- assembledPrompt includes project memory content
- Returns all 5 layers in response

**Result:** 24/24 Round 8 tests passing. All test against the shipped implementation.

## 09:10 — Pre-existing test failure triage

Investigated 14 failures across 4 files (`queries.test.ts`, `channels.test.ts`, `entities.test.ts`, `metadata.test.ts`).

**Root cause:** Legacy test files (Rounds 1-3) don't import `setup.ts`, so the `vi.mock('../db/index.js')` never applies. They run against the real `klatch.db`. Evidence: failure counts change between runs as rows accumulate in the real DB.

**Fix:** Single-line fix — add `import './setup.js'` to each file. The tests themselves are correct.

**Severity:** Low — test infrastructure only, no production code affected.

Full writeup: `docs/test-failure-triage-2026-03-17.md`
