# xian Attention Queue

Append-only list of items either Dispatch has flagged for xian's input. xian reads this at session-open on either machine. Items get moved to Resolved when handled.

Format: `- [date] from {DK|DinP}: {brief description} — context: {file path or memo ref}`

---

## Active

- [2026-05-05] from DK: cadence-rigor question from DinP daily 2026-04-28 still un-replied (7 days old) — *"how strict do you want the daily-memo discipline when substantive same-day mail to you already covers the daily-state function?"* — context: `mail/memo-dispatch-dinp-to-dispatch-kind-daily-2026-04-28.md` "Anything for you" §1; carried in 4/29, 4/30, 5/01 dailies; ack at `mail/memo-dispatch-kind-to-dispatch-dinp-ack-pending-strategic-2026-05-05.md`.
- [2026-05-05] from DK: branch-discipline-as-constellation question from DinP daily 2026-04-28 still un-replied (7 days old) — *"Worth distributing as a methodology note to PM and Klatch? I can draft as a brief-class artifact if useful, or you can absorb if you'd rather centralize the pattern on your side."* Likely partially self-resolving (PA's 4/29 v1.0 synthesis + Janus's role-evolution to constellation backstop), but original "should Janus draft a brief-class artifact" still hasn't gotten explicit DK/xian read. Context: `mail/memo-dispatch-dinp-to-dispatch-kind-daily-2026-04-28.md` "Anything for you" §2; ack at same file as above.
- [2026-05-05] from DK: backfill-discipline rule from DinP daily 2026-05-01 still un-replied (4 days old) — *"Open to your input on whether retrospective backfill counts as 'filing' or whether the cadence should accept skip-days as long as they're acknowledged."* De-facto practice (next Janus session backfills from logs / cross-side traffic / git history) has been used three times; question is whether to formalize. Context: `mail/memo-dispatch-dinp-to-dispatch-kind-daily-2026-05-01.md` "Anything for you" §1; ack at same file as above.
- [2026-05-05] from DK: `dinp-daily-memo` task ran today; same automation drift as 2026-05-04 — memo written to `mail/memo-dispatch-kind-to-dispatch-dinp-daily-2026-05-05.md` on kindbook (uncommitted, untracked). `git add` failed at `.git/index.lock` (leftover from yesterday's failed run, mount permits no unlink — `rm` returns "Operation not permitted"). Sandbox-side fix not available; needs host-side intervention. Combined with the 5/4 daily and the 5/05 strategic-questions ack, three DK-authored mail files now sit untracked on kindbook awaiting commit + push via feature-branch + PR. Action: same as the 5/04 entry below — commit + push the three memos, remove `.git/index.lock`, update `dinp-daily-memo` and `dinp-inbox-check` SKILL.md to feature-branch + PR pattern.
- [2026-05-04] from DK: `dinp-daily-memo` task ran but could not commit/push — memo written to `mail/memo-dispatch-kind-to-dispatch-dinp-daily-2026-05-04.md` on kindbook (uncommitted, untracked). Two root causes: (a) Cowork Linux sandbox has no `osascript` and mount permits create/append/overwrite-existing only (no delete/unlink), so git operations fail on `.git/index.lock`; (b) per `standards/COWORK-TO-CODE-TASK-PUSH-PATTERN.md` (May 2026 update), direct-to-main pushes are blocked — new canonical flow is feature branch + PR. The DK SKILL.md for `dinp-daily-memo` still says `git push origin main`; needs update. Action: commit + push the memo via feature-branch + PR pattern, and update the SKILL.md (and the symmetric `dinp-inbox-check` SKILL.md if it has the same shape). Same drift may apply to DinP-side `dk-daily-memo` task. **Working-tree note:** `git status` will also show `M standards/COWORK-TO-CODE-TASK-PUSH-PATTERN.md` and `?? mail/test-write-can-i.txt` as artifacts of this run — the standards-file diff is a partial-checkout sticky (the in-flight `dk/2026-05-05-push-pattern-verify-pr` branch's content remained on disk after a `git checkout main` whose unlink was denied by mount permissions); the test stub is a sandbox capability probe; neither belongs in any DK commit. Stage by named path only (`git add mail/memo-dispatch-kind-to-dispatch-dinp-daily-2026-05-04.md xian-attention-queue.md`), and once the memo lands, the test stub can be removed host-side.

## Resolved

(none yet)

